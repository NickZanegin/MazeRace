using UnityEngine;
using LionStudios.Suite.Debugging;

public class LIonAnalitics : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        LionDebugger.Hide();
    }
}
